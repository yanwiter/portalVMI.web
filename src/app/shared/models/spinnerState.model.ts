/**
 * Interface que representa o estado do spinner.
 *
 * @interface SpinnerStateModel
 */
export interface SpinnerStateModel {
  /**
   * Indica se o spinner deve ser exibido ou não.
   *
   * `true` para exibir o spinner, `false` para ocultá-lo.
   */
  show: boolean;
}
